import { useBettingPortfolio } from "@/hooks/useBettingPortfolio";

export function BettingPortfolio() {
  return (
    <div>
      {/* ポートフォリオ最適化とUIを実装予定 */}
      <p>ポートフォリオ画面（実装中）</p>
    </div>
  );
} 